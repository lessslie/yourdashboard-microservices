# ms-yourdashboard-email/.env.example

# Copiar este archivo como .env y completar con valores reales

# ================================
# Database Configuration
# ================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=ms_yourdashboard_auth

# ================================
# Application
# ================================
PORT=3002
NODE_ENV=development

# ================================
# 🔄 CRON SYNC CONFIGURATION
# ================================
# Activar/Desactivar sync automático
SYNC_CRON_ENABLED=true

# Horarios de sincronización (formato CRON)
# Días de semana: cada 10 minutos
SYNC_WEEKDAY_SCHEDULE=*/10 * * * 1-5

# Fines de semana: cada 4 horas  
SYNC_WEEKEND_SCHEDULE=0 */4 * * 0,6

# Para testing: cada minuto (comentado por defecto)
# SYNC_TEST_SCHEDULE=*/1 * * * *

# Límites de sincronización
SYNC_MAX_EMAILS_PER_ACCOUNT=30
SYNC_MAX_ACCOUNTS_PER_RUN=50

# ================================
# 📧 GMAIL SYNC SETTINGS
# ================================
# Solo sincronizar cuentas activas en los últimos X días
SYNC_ACTIVE_ACCOUNTS_DAYS=7

# Reintentos en caso de error
SYNC_MAX_RETRIES=3
SYNC_RETRY_DELAY_MS=1000

# Email Search Mode Configuration
# true = Base de Datos (rápido - 50ms)//aun en prueba
# false = Gmail API (lento - 2-5s)
USE_DATABASE_MODE=true